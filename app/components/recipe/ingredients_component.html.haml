.flex.flex-col.gap-y-4
  .flex.flex.gap-y-2.items-baseline
    .grow
      %h2= RecipeIngredient.model_name.human(count: 0)
    .shrink-0.text-gray-400.text-xs
      = t('recipe.servings_with_count', count: @recipe.servings)

  = render Recipe::IngredientsListComponent.new(recipe: @recipe)
