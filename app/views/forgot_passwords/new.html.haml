.flex.flex-col.gap-y-8
  %h1 Forgot password?

  = form_with(model: @form, url: forgot_passwords_url) do |f|
    .bg-white.rounded.overflow-hidden.shadow
      .pt-2.pb-4.px-4.flex.flex-col.gap-y-4
        .flex.flex-col.gap-y-1
          = f.label :email, class: 'label'
          = f.email_field :email, class: 'input'
          = render 'layouts/forms/field_errors', errors: f.object.errors, key: :email

        .flex.flex-col.gap-y-2
          = f.button type: :submit, class: 'button gap-x-1' do
            .h-4.w-4
              = raw heroicon :mail
            Reset password

          .text-center
            = link_to 'Cancel', root_path, class: 'shy-link text-xs'
