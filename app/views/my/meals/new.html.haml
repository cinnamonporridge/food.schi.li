- if @form.meal.recipe.present?
  %h1 Add portion to recipe
  .text-gray-500.text-xs.mb-5.mt-3.tracking-wide
    Add a portion to
    %span.text-gray-600= @form.meal.recipe.name
    on
    %span.text-gray-600= @journal_day.decorate.display_date
- else
  %h1 Add portion meal
= render 'portion_form', form: @form
