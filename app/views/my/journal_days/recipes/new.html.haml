.flex.flex-col.gap-y-4
  %h1 Add recipe to journal day

  = form_with(model: [:my, @form]) do |f|
    .bg-white.rounded.overflow-hidden.shadow.divide-y.divide-gray-200
      .pt-2.pb-4.px-4.flex.flex-col.gap-y-2
        .flex.flex-col
          = f.label :recipe_name, class: 'label'
          = f.text_field :recipe_name, list: :recipe_name_list, class: 'input'
          = render 'layouts/forms/field_errors', errors: f.object.errors, key: :recipe_name
          %datalist#recipe_name_list
            = options_for_select JournalDayRecipeDecorator.recipes_collection(@form.journal_day.user)

        .flex.flex-col
          = f.label :servings, class: 'label'
          = f.text_field :servings, class: 'input', min: 1
          = render 'layouts/forms/field_errors', errors: f.object.errors, key: :servings

      .p-4.flex.flex-col.gap-y-2
        = f.button type: :submit, class: 'button gap-x-2' do
          .h-4.w-4
            = raw heroicon :'plus-sm'
          Add recipe to journal day

        .text-center
          = link_to 'Cancel', [:my, @form.journal_day], class: 'shy-link text-xs'
