= provide :page_title, 'Invite'
= simple_form_for(@user) do |form|
  = form.input :email, placeholder: 'Email'
  = form.submit 'Create user', class: 'btn btn-success'
  = link_to 'Cancel', users_path
