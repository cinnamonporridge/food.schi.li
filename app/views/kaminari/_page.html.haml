%li.pagination-page{ class: "#{'current' if page.current?}"}
  - if page.current?
    %span.p-1= page
  - else
    = link_to page, url, { remote: remote, rel: page.rel, class: 'text-green-600 hover:text-green-400 transition duration-300 p-1' }
